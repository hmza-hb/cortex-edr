# Webhooks

Configure a webhook endpoint in your repository settings to receive real-time push events whenever a critical vulnerability triggers.

Payloads are signed with a unique `X-Cortex-Signature` using your account's webhook secret.
