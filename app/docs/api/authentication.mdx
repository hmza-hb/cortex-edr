# API Authentication

All requests must use a Bearer token generated from the developer settings page. Keep this token secure as it has read-write access matching your current tier.

```bash
curl -H "Authorization: Bearer crtx_..." https://api.cortex-edr.com/v1/scan
```
